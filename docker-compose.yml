services:
  ayla-bridge:
    container_name: ayla-bridge
    build: .
    environment:
      - BIND_IP=192.168.1.8
      - BIND_PORT=10275
      - DEVICES_PATH=/app/data/devices.json
    volumes:
      - ./json:/app/data
    network_mode: host
    restart: unless-stopped
